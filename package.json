{
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "esbuild --bundle --watch --outdir=. src/index.js",
    "build": "esbuild --bundle --minify --outdir=. src/index.js"
  },
  "devDependencies": {
    "esbuild": "^0.9.6"
  }
}
